default:
    formatters:
        pretty:
            verbose:  true
            paths:    false
            snippets: false

    suites:
        web:
            contexts:
                - Resources\Behat\WebContext
    extensions:
        Behat\MinkExtension:
            sessions:
                default:
                    symfony2: ~
        Behat\Symfony2Extension: ~